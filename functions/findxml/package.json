{
  "name": "cut",
  "version": "0.0.1",
  "description": "A Lambda function that is triggered when content is uploaded to S3 and searches for an XML that describes the content of the newspaper",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sean M. Tracey",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.4.7",
    "debug": "^2.2.0",
    "dotenv": "^2.0.0",
    "node-babel": "^0.1.2"
  },
  "engines": {
    "node": "4.3.0"
  }
}
